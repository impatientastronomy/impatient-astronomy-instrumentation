# Core scientific stack
numpy
pandas

# Configuration
pyyaml

# Image I/O and processing
tifffile
opencv-python; sys_platform == "darwin"          # macOS: headless wheel is broken (missing libxcb)
opencv-python-headless; sys_platform == "linux"  # Linux/Pi: headless avoids SDL2 conflict with pygame

# Display
pygame

# ZWO ASI camera SDK Python bindings
zwoasi

# Development / testing
pytest
